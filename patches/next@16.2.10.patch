diff --git a/dist/build/templates/app-page.js b/dist/build/templates/app-page.js
index e0e16e8cdcb98beb66af51c924921b6d2e2fa904..2135e05e7f47eedf8b7932edf2bf95a6dd6d6071 100644
--- a/dist/build/templates/app-page.js
+++ b/dist/build/templates/app-page.js
@@ -443,7 +443,19 @@ async function handler(req, res, ctx) {
     };
     try {
         const varyHeader = routeModule.getVaryHeader(resolvedPathname, interceptionRoutePatterns);
-        res.setHeader('Vary', varyHeader);
+        const varyFields = new Map();
+        for (const value of [
+            res.getHeader('Vary'),
+            varyHeader
+        ].flat()){
+            for (const field of String(value ?? '').split(',')){
+                const normalized = field.trim();
+                if (normalized) varyFields.set(normalized.toLowerCase(), normalized);
+            }
+        }
+        res.setHeader('Vary', [
+            ...varyFields.values()
+        ].join(', '));
         let parentSpan;
         const invokeRouteModule = async (span, context)=>{
             const nextReq = new _node.NodeNextRequest(req);
diff --git a/dist/esm/build/templates/app-page.js b/dist/esm/build/templates/app-page.js
index 0cec08429603871865933de0a5ae4dd8f0d9a0d3..b0096de74e57d02f3f6461cee52c5ca029908e54 100644
--- a/dist/esm/build/templates/app-page.js
+++ b/dist/esm/build/templates/app-page.js
@@ -434,7 +434,19 @@ export async function handler(req, res, ctx) {
     };
     try {
         const varyHeader = routeModule.getVaryHeader(resolvedPathname, interceptionRoutePatterns);
-        res.setHeader('Vary', varyHeader);
+        const varyFields = new Map();
+        for (const value of [
+            res.getHeader('Vary'),
+            varyHeader
+        ].flat()){
+            for (const field of String(value ?? '').split(',')){
+                const normalized = field.trim();
+                if (normalized) varyFields.set(normalized.toLowerCase(), normalized);
+            }
+        }
+        res.setHeader('Vary', [
+            ...varyFields.values()
+        ].join(', '));
         let parentSpan;
         const invokeRouteModule = async (span, context)=>{
             const nextReq = new NodeNextRequest(req);
