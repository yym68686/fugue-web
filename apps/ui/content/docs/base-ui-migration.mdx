---
title: Base UI migration
description: Moving legacy component composition to the active COSS model.
---

Replace nested interactive elements and `asChild` with Base UI `render`. Keep action items as buttons and navigation items as links. Validate final DOM semantics, focus return, Escape behavior and keyboard navigation after each migration.

Legacy `Content` aliases may exist only during a time-bounded consumer migration; new code uses Popup or Panel names.
